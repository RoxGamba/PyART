# Requirements for most basic library
# Note: numpy<2.0 required — pycbc>=2.4.0 uses np.array(..., copy=False) which
# changed semantics in numpy 2.x and raises ValueError at runtime.
numpy>=1.16.0,<2.0,!=1.19.0
scipy>=1.13.0
rich>=13.7.0
matplotlib>=3.7.3
pandas>=2.1.4
astropy>=5.0.0
setuptools

# GW package dependencies
pycbc>=2.4.0
# teobresums>=4.4.0
# gw-eccentricity>=1.0.4
# mayawaves>=2024.4.25
lalsuite>=7.22

# SXS catalog support (optional; needed for downloading SXS simulations)
# sxs>=2025.0 requires numpy>=2.0 which is incompatible with pycbc; pin <2025
sxs>=2024.0.0,<2025.0
romspline

# Documentation
sphinx>=4.2.0
myst-parser>=2.0.0
myst-nb>=1.3.0
furo>=2023.9.10
sphinx-math-dollar
sphinx-autoapi>=3.0.0
