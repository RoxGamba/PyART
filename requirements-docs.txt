# Extra dependencies for building the Sphinx docs (make -C docs html).
# Not needed to install or use the library itself.
sphinx>=4.2.0
myst-parser>=2.0.0
myst-nb>=1.3.0
furo>=2023.9.10
sphinx-math-dollar
sphinx-autoapi>=3.0.0

# Used by the executed tutorial notebooks (intro_to_waveforms.ipynb,
# phase_alignment.ipynb).
seaborn
